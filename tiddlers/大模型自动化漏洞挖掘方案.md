主要是记录一些公开的成熟的 CRS 方案。


[[什么是 CRS]]

## AIxCC 第三名：Theori / RoboDuck 的公开方案

Theori 设计的这套 CRS 叫做 RoboDuck，他们自己把它描述成一个偏 LLM-first 的系统：尽量让 LLM 参与漏洞发现、PoV 生成、补丁生成、crash 根因分析等环节，同时保留 fuzzing 和静态分析作为信号来源与兜底。

### 1. 发现漏洞

RoboDuck 的第一层是**候选发现**。Theori 公开提到用了 Infer 这类静态分析工具，也用了 libFuzzer harness 和 coverage 反馈；但他们也承认传统静态分析在这种场景下假阳性很高，所以不能直接把所有报告都当成漏洞。